<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <item
        android:id="@+id/sortMenuItem"
        android:icon="@android:drawable/ic_menu_sort_by_size"
        android:title="@string/sort"
        app:showAsAction="ifRoom"
        tools:ignore="sort">
        <menu>
            <item
                android:id="@+id/actionSortByWins"
                android:orderInCategory="1"
                android:title="@string/sort_by_wins"
                app:showAsAction="never"/>
            <item
                android:id="@+id/actionSortByLosses"
                android:orderInCategory="2"
                android:title="@string/sort_by_losses"
                app:showAsAction="never"/>
            <item
                android:id="@+id/actionSortAlphabetically"
                android:orderInCategory="1"
                android:title="@string/sort_alphabetically"
                app:showAsAction="never"/>
        </menu>
    </item>
</menu>